# =============================================================================
# ILKE: vLLM'in KENDI transitive bagimliliklarini (fastapi, uvicorn, pydantic,
# httpx, openai, outlines, torch, transformers...) burada SABITLEMEYIZ. Bu
# paketler icin yalnizca SAFIR kodunun fiilen ihtiyac duydugu ALT SINIRLAR
# verilir, UST SINIR verilmez — nihai surumu vLLM'in kendi dependency
# resolver'i secer. Boylece vllm hangi (guncel) surume cozulurse cozulsun,
# onun istedigi fastapi/pydantic/openai/outlines surumleriyle CATISMAYIZ.
#
# `--no-deps` ile kismi/manuel kurulum YAPMAYIN: eski bir requirements.txt
# kopyasiyla birlikte kullanilirsa vllm'i sessizce ESKI bir surume dusurup
# kirik transitive bagimliliklara (orn. outlines<0.1.0 -> pyairports) geri
# doner. Temiz kurulum icin KURULUM.md'deki Faz 1/Faz 2 prosedurunu izleyin.
# =============================================================================
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7,<3
PyYAML>=6.0.1
httpx>=0.27
# python-dotenv: .env icindeki EVREN_* anahtarlari SURECIN BASINDA yuklenir
# (src/main.py ve src/utils/config_loader.py ilk satirlarda `load_dotenv()`
# cagirir) - eksikse temiz bir kurulumda uygulama ACILISTA ImportError verir.
python-dotenv>=1.0.1

# --- Hafif Suzgec Katmani (CPU) ---
opencv-python-headless>=4.10.0.84
numpy>=1.26,<3

# --- VLM / LLM (vLLM servis altyapisi + istemci) ---
# NOT: vllm Linux tabanli bir sunucu paketidir. Windows/Mock modunda atlanir.
vllm>=0.17.0; sys_platform != 'win32'
outlines>=0.1.0
openai>=1.35

# --- Ajan ve Muhakeme Katmani (LangGraph) ---
langgraph>=0.1.9
langchain-core>=0.2.28
langchain-openai>=0.1.17

# --- Hibrit Bellek Katmani (Embedding: EVREN /v1/embeddings; Vektor deposu: Qdrant) ---
qdrant-client>=1.9.0
# sentence-transformers: yalnizca opsiyonel LOKAL Cross-Encoder (src/rag/local_cross_encoder_reranker.py,
# production'da VARSAYILAN DEGIL - bkz. src/main.py) standalone/benchmark kullanimi icin kalir.
sentence-transformers>=3.0.0

# --- Rapor Disa Aktarma (PDF) ---
# reportlab: GET /history/{job_id}/report.pdf tarafindan kullanilan
# ReportExporter.to_pdf() (src/ui/report_export.py) icin GERCEK bagimlilik —
# bu siniif hem Operator Paneli (Streamlit) hem artik ana API tarafindan
# PAYLASILIR (kod TEKRARLANMADI). Surum, requirements-dashboard.txt'teki ile
# AYNI tutulur.
reportlab>=4.2.2

# --- SAFIR Asistan: kullanici belge yukleme (Adim 4) ---
# pypdf: GERCEK, sayfa-bazli PDF metin cikarimi (src/memory/document_extraction.py).
# Saf Python (C uzantisi/derleyici GEREKMEZ), aktif bakimda, hafif.
pypdf>=6.0.0
# python-docx: GERCEK DOCX metin cikarimi (ayni modul). Bu iki paket, kucuk
# yerel modellerin/agentin ihtiyaci olan TEK is: dosya -> duz metin.
python-docx>=1.1.2
# python-multipart: FastAPI/Starlette'in `UploadFile` (multipart form) istek
# govdesini ayristirmasi icin GEREKLI (belge yukleme uc noktasi olmadan
# gerek yoktu; simdi GERCEK bir bagimlilik). Ust sinir yok (fastapi/starlette
# ile ayni ilke — bkz. dosyanin basindaki ILKE notu).
python-multipart>=0.0.9

# Not: Operator Paneli (Streamlit) hala ayri, opsiyonel bir surectir (bkz.
# requirements-dashboard.txt, kendi izole venv'inde calisir) — streamlit'in
# kendisi burada tutulmaz, yalnizca reportlab artik ikisi arasinda PAYLASILAN
# gercek bir ana-API bagimliligidir.
